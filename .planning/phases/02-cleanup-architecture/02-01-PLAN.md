---
phase: 02-cleanup-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/ui/ (entire directory deleted)
  - src/app/components/Reel.tsx (deleted)
  - src/app/components/figma/ (entire directory deleted)
  - src/lib/utils.ts (created)
  - package.json
  - package-lock.json
  - bun.lock
  - postcss.config.mjs (deleted)
  - vite.config.ts
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, QUAL-03]

must_haves:
  truths:
    - 'src/app/components/ui/ directory does not exist'
    - 'src/app/components/Reel.tsx does not exist'
    - 'src/app/components/figma/ directory does not exist'
    - 'postcss.config.mjs does not exist'
    - 'npm ls --depth=0 --prod shows 10-15 production dependencies'
    - 'package.json name field is stealinglight-hk'
    - 'package.json has no pnpm.overrides, peerDependencies, or peerDependenciesMeta blocks'
    - 'npm run build completes with zero errors'
  artifacts:
    - path: 'src/lib/utils.ts'
      provides: 'cn() class merging utility relocated from ui/utils.ts'
      exports: ['cn']
      contains: 'twMerge(clsx(inputs))'
    - path: 'package.json'
      provides: 'Clean dependency manifest with correct name and no stale blocks'
      contains: 'stealinglight-hk'
  key_links:
    - from: 'src/lib/utils.ts'
      to: 'clsx, tailwind-merge'
      via: 'import'
      pattern: 'import.*clsx.*twMerge'
---

<objective>
Delete all 44 unused shadcn/ui component files, 2 dead application components, and the empty postcss config. Relocate the cn() utility to src/lib/utils.ts. Then overhaul package.json: remove 50+ unused packages, add Fontsource font packages and new ESLint tooling, rename from @figma/my-make-file to stealinglight-hk, remove stale pnpm/peer blocks, update lint scripts to drop ESLINT_USE_FLAT_CONFIG flag, and regenerate both lockfiles.

Purpose: Establish the lean dependency baseline that all subsequent Phase 2 plans build on. Files must be deleted before packages are removed (otherwise TypeScript reports import errors in files about to be deleted). Package.json must be fully updated before Plans 02 and 03 can install/use the new dependencies.

Output: Clean codebase with src/lib/utils.ts, ~10 production deps, both lockfiles regenerated, build passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cleanup-architecture/02-CONTEXT.md
@.planning/phases/02-cleanup-architecture/02-RESEARCH.md

<interfaces>
<!-- Current cn() utility to relocate (source of truth for new src/lib/utils.ts) -->
From src/app/components/ui/utils.ts:
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
return twMerge(clsx(inputs));
}

````

<!-- Current package.json lint scripts that need updating -->
From package.json (lines 10-11):
```json
"lint": "ESLINT_USE_FLAT_CONFIG=false eslint .",
"lint:fix": "ESLINT_USE_FLAT_CONFIG=false eslint --fix --max-warnings=0",
````

<!-- Current lint-staged config (in package.json lines 109-115) -->

```json
"lint-staged": {
  "*.{ts,tsx}": ["npm run lint:fix"],
  "*.{json,md,css}": ["prettier --write"]
}
```

<!-- Vite config with Figma artifact comment to clean -->

From vite.config.ts:

```typescript
// The React and Tailwind plugins are both required for Make, even if
// Tailwind is not being actively used – do not remove them
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead files and relocate cn() utility</name>
  <files>
    src/app/components/ui/ (48 files, entire directory)
    src/app/components/Reel.tsx
    src/app/components/figma/ImageWithFallback.tsx
    src/app/components/figma/ (directory)
    src/lib/utils.ts (created)
    postcss.config.mjs (deleted)
    vite.config.ts
  </files>
  <read_first>
    src/app/components/ui/utils.ts
    vite.config.ts
    postcss.config.mjs
  </read_first>
  <action>
1. Delete the entire `src/app/components/ui/` directory (all 48 files: 44 shadcn/ui components + utils.ts + use-mobile.ts + any others). Use `rm -rf src/app/components/ui/`. Per D-01, zero files remain -- the directory itself is removed.

2. Delete `src/app/components/Reel.tsx` (dead component, zero imports). Per QUAL-03.

3. Delete the entire `src/app/components/figma/` directory (contains only `ImageWithFallback.tsx`, which has zero imports). Per QUAL-03.

4. Create `src/lib/utils.ts` with the cn() utility relocated from the deleted ui/utils.ts. Per D-02. The exact content:

```typescript
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Note: Use single quotes to match the project's Prettier config (singleQuote: true).

5. Delete `postcss.config.mjs` (exports empty object `{}`, Tailwind v4 uses @tailwindcss/vite plugin which handles PostCSS internally -- this file is unnecessary). Claude's discretion per CONTEXT.md.

6. Update the comment in `vite.config.ts` (lines 8-9) from the Figma artifact:
   - REMOVE: `// The React and Tailwind plugins are both required for Make, even if`
   - REMOVE: `// Tailwind is not being actively used – do not remove them`
   - REPLACE WITH: nothing (just delete both comment lines, the plugins speak for themselves)
     </action>
     <verify>
     <automated>test ! -d src/app/components/ui && test ! -f src/app/components/Reel.tsx && test ! -d src/app/components/figma && test -f src/lib/utils.ts && test ! -f postcss.config.mjs && ! grep -q "Make" vite.config.ts && echo "PASS" || echo "FAIL"</automated>
     </verify>
     <acceptance_criteria>
   - `src/app/components/ui/` directory does not exist: `test ! -d src/app/components/ui`
   - `src/app/components/Reel.tsx` does not exist: `test ! -f src/app/components/Reel.tsx`
   - `src/app/components/figma/` directory does not exist: `test ! -d src/app/components/figma`
   - `src/lib/utils.ts` exists and contains `export function cn`: `grep -q "export function cn" src/lib/utils.ts`
   - `src/lib/utils.ts` imports from clsx and tailwind-merge: `grep -q "clsx" src/lib/utils.ts && grep -q "twMerge" src/lib/utils.ts`
   - `postcss.config.mjs` does not exist: `test ! -f postcss.config.mjs`
   - `vite.config.ts` does not contain "Make": `! grep -q "Make" vite.config.ts`
     </acceptance_criteria>
     <done>All 48 shadcn/ui files + directory deleted, Reel.tsx deleted, figma/ directory deleted, cn() utility lives at src/lib/utils.ts, postcss.config.mjs deleted, vite.config.ts Figma comment removed</done>
     </task>

<task type="auto">
  <name>Task 2: Package.json overhaul and lockfile regeneration</name>
  <files>
    package.json
    package-lock.json
    bun.lock
  </files>
  <read_first>
    package.json
  </read_first>
  <action>
Make ALL of the following changes to package.json in a single edit:

**A. Rename package** (per CLEAN-03, D-03):

- Change `"name": "@figma/my-make-file"` to `"name": "stealinglight-hk"`

**B. Remove stale config blocks** (per CLEAN-03):

- Delete the entire `"pnpm"` block (contains `"overrides": { "vite": "6.3.5" }` -- stale, project uses Vite 7.3.1)
- Delete the entire `"peerDependencies"` block (Figma template artifact -- this is not a published package)
- Delete the entire `"peerDependenciesMeta"` block (same reason)

**C. Remove unused production dependencies** (per CLEAN-02, D-03):
Remove ALL of these from `"dependencies"`:

- `@emotion/react`, `@emotion/styled`, `@mui/icons-material`, `@mui/material` (MUI -- never imported)
- All 26 `@radix-ui/react-*` packages: accordion, alert-dialog, aspect-ratio, avatar, checkbox, collapsible, context-menu, dialog, dropdown-menu, hover-card, label, menubar, navigation-menu, popover, progress, radio-group, scroll-area, select, separator, slider, slot, switch, tabs, toggle, toggle-group, tooltip
- `cmdk`, `date-fns`, `embla-carousel-react`, `input-otp`, `next-themes`, `react-day-picker`, `react-dnd`, `react-dnd-html5-backend`, `react-hook-form`, `react-resizable-panels`, `react-responsive-masonry`, `react-slick`, `recharts`, `vaul`

**D. Add new production dependencies** (per PERF-01, D-07):

- `@fontsource-variable/inter`: `^5.2.8`
- `@fontsource-variable/space-grotesk`: `^5.2.10`

**E. Remove old ESLint dev dependencies** (per CLEAN-04):

- Remove `@typescript-eslint/eslint-plugin` from devDependencies
- Remove `@typescript-eslint/parser` from devDependencies

**F. Add new ESLint dev dependencies** (per CLEAN-04, D-10):

- `@eslint/js`: `^10.0.1`
- `globals`: `^17.4.0`
- `typescript-eslint`: `^8.58.0`

**G. Update lint scripts** (per CLEAN-04, D-10):

- Change `"lint"` from `"ESLINT_USE_FLAT_CONFIG=false eslint ."` to `"eslint ."`
- Change `"lint:fix"` from `"ESLINT_USE_FLAT_CONFIG=false eslint --fix --max-warnings=0"` to `"eslint --fix --max-warnings=0"`

After editing package.json, regenerate lockfiles:

1. Run `npm install` to update package-lock.json (this also installs the new deps)
2. Run `bun install` to update bun.lock
3. Run `npm run build` to verify the Vite build succeeds with zero errors
4. Run `npm ls --depth=0 --prod 2>/dev/null | grep -c "^[├└]"` to count production deps (should be 10-15)

Note: The lint-staged config in package.json (`"*.{ts,tsx}": ["npm run lint:fix"]`) does NOT need changes -- it calls `npm run lint:fix` which will automatically use the updated script.
</action>
<verify>
<automated>grep -q '"stealinglight-hk"' package.json && ! grep -q '"pnpm"' package.json && ! grep -q '"peerDependencies"' package.json && ! grep -q '@mui' package.json && ! grep -q '@radix-ui' package.json && ! grep -q 'recharts' package.json && grep -q '@fontsource-variable/inter' package.json && grep -q 'typescript-eslint' package.json && ! grep -q 'ESLINT_USE_FLAT_CONFIG' package.json && npm run build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
</verify>
<acceptance_criteria> - Package name is stealinglight-hk: `grep -q '"name": "stealinglight-hk"' package.json` - No pnpm block: `! grep -q '"pnpm"' package.json` - No peerDependencies block: `! grep -q '"peerDependencies"' package.json` - No MUI packages: `! grep -q '@mui' package.json` - No Radix packages: `! grep -q '@radix-ui' package.json` - No recharts/react-hook-form/cmdk/vaul: `! grep -q 'recharts\|react-hook-form\|cmdk\|vaul' package.json` - Fontsource Inter added: `grep -q '@fontsource-variable/inter' package.json` - Fontsource Space Grotesk added: `grep -q '@fontsource-variable/space-grotesk' package.json` - typescript-eslint added: `grep -q '"typescript-eslint"' package.json` - @eslint/js added: `grep -q '@eslint/js' package.json` - globals added: `grep -q '"globals"' package.json` - Old ts-eslint plugin removed: `! grep -q '@typescript-eslint/eslint-plugin' package.json` - Old ts-eslint parser removed: `! grep -q '@typescript-eslint/parser' package.json` - ESLINT_USE_FLAT_CONFIG flag removed from scripts: `! grep -q 'ESLINT_USE_FLAT_CONFIG' package.json` - lint script is clean: `grep '"lint":' package.json | grep -q '"eslint ."'` - Build succeeds: `npm run build` exits 0 - Both lockfiles exist: `test -f package-lock.json && test -f bun.lock`
</acceptance_criteria>
<done>package.json has name "stealinglight-hk", ~10 production deps, no stale blocks, new font and ESLint deps added, lint scripts updated, both lockfiles regenerated, build passes</done>
</task>

</tasks>

<verification>
1. `test ! -d src/app/components/ui` -- ui directory fully removed
2. `test -f src/lib/utils.ts && grep -q "export function cn" src/lib/utils.ts` -- cn() utility relocated
3. `npm run build` -- Vite build succeeds with no errors
4. `grep -q '"stealinglight-hk"' package.json` -- package renamed
5. `! grep -q 'ESLINT_USE_FLAT_CONFIG' package.json` -- flag removed from scripts
6. `npm ls --depth=0 --prod 2>/dev/null | wc -l` -- production dependency count is reasonable
</verification>

<success_criteria>

- Zero files remain in src/app/components/ui/ (directory itself deleted)
- src/lib/utils.ts exports cn() function
- package.json name is "stealinglight-hk" with ~10 production dependencies
- No MUI, Radix, or unused library packages in package.json
- ESLINT_USE_FLAT_CONFIG flag removed from all scripts
- npm run build completes successfully
- Both package-lock.json and bun.lock are regenerated and committed
  </success_criteria>

<output>
After completion, create `.planning/phases/02-cleanup-architecture/02-01-SUMMARY.md`
</output>
